# 虚拟环境
.venv/

# Python 缓存
__pycache__/
*.pyc

# 运行时/个人配置与校准数据（分辨率相关、含屏幕截图，不进仓库）
debug/
config.json
templates/
samples/

# PyInstaller 打包产物（中间 build/ 与最终 dist/ 不提交）
build/
dist/
*.log

# 发布用压缩包（release zip）
DBD下钩计时助手.zip

# 打包脚本与说明：仅供本机手动打包使用，不入仓库
dbdtimer.spec
打包成exe.bat
发布说明.txt
